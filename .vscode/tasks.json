{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "yui:build",
      "type": "shell",
      "command": "pnpm run yui:build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
